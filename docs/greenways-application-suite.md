# Current Greenways Spaces and Flow screen laboratory

Primary issue: `greenways-ai/visual-language#58`

Related product laboratories:

- Spaces: `greenways-ai/visual-language#59`
- Flow Project Control Room: `greenways-ai/visual-language#36`
- Neutral interface contract: `greenways-ai/visual-language#41`

## Current product law

The foreground application launcher contains exactly:

```text
Spaces    understand
Flow      coordinate
```

The Greenways Fabric remains beneath those applications. Settings, Connections, Identity, Storage, Packages, Activity, Diagnostics, and Recovery are subordinate system surfaces rather than additional foreground applications.

Earlier Today, Workrooms, Studio, Campaigns, Packages, Keyring, Receipts, Research, and Build concepts remain useful design and compatibility evidence. They do not reappear as current application identities. Imagine and World remain unactivated internal conformance targets and are absent from current launcher, recents, typed search, onboarding, ordinary command palettes, and CLI help.

## Screen family

### Suite overview

Shows the two current applications, recent owned records, typed global search, human attention, system-surface separation, and a hidden future-target conformance specimen. Search results preserve application owner, record kind, stable identity, freshness, and authority state.

### Native desktop

Uses Spaces as the primary workbench study. One exact selected record survives map, source, evidence, outline, timeline, table, and document modes. The map differentiates visual association, candidate relation, reviewed assertion, canonical semantic proposal, and Flow reference without relying on colour alone.

A secondary Flow plate demonstrates project coordination without making infrastructure the product. The selected Flow project keeps agent, session, host, work, claim, handoff, artifact, and external truth separate. Foreman appears only under technical disclosure.

### Browser workspace

Uses Flow as a full-page Project Control Room. It shows project identity, agents, work and claims, observed hosts, interventions, attributable activity, and exact Spaces source context. Requested capability, observed capability, and excluded authority remain separate facts.

### Browser companion

Shows a bounded Spaces current-page capture and Flow handoff review beside the page. It can identify an exact target Space, preview included and excluded browser data, inspect provenance, prepare a handoff, and open the full application. The compact popup remains limited to health, current context, and open/capture commands. Neither compact surface pretends to be a full Space map or Flow Control Room.

### CLI companion

Shows current commands only:

```text
greenways spaces ...
greenways flow projects
greenways flow project <project-id>
greenways flow work <project-id>
```

Research and Build appear only under compatibility disclosure and must resolve to exact current identities or fail explicitly. Future application commands are absent from ordinary help and completion.

### Spaces ↔ Flow handoff

The current loop is:

```text
Space question and cited brief
  -> Flow project work
  -> reported artifact
  -> explicit human selection
  -> returned reference in the same Space
```

The specimen preserves exact Space, brief, question, source anchors, Flow project, work item, optional grouping, idempotency, lifecycle, result, and return target. Included context and excluded authority are adjacent. Prepared, approval-required, ready, accepted, creating, received, selected, and completed are separate states; partial, rejected, cancelled, incompatible, stale, and failed are also visible.

## Historical feature continuity

Useful earlier screen capabilities survive under their current owners:

| Earlier idea | Current owner and treatment |
| --- | --- |
| Daily focus and useful signals | Suite attention projections and Flow interventions |
| Persistent workrooms | Spaces source/conversation context and Flow project collaboration |
| Studio source relationships | Spaces cited brief composition and returned artifacts |
| Campaign milestones and approvals | Flow project work, interventions, and externally verified evidence |
| Package compatibility | Subordinate Packages system surface and technical disclosure |
| Keyring grants and recovery | Identity, Connections, and Recovery system surfaces |
| Receipt traces | Spaces evidence and Flow attributable activity/evidence |

No historical feature creates a duplicate record owner or a third current application.

## Visual contract

- neutral paper/graphite canvas, rails, panels, tables, and dialogs;
- the Greenways mosaic remains the strongest multicolour identity moment;
- Spaces and Flow use small durable application signals only;
- application signal and semantic success, warning, danger, offline, approval, and uncertainty remain separate;
- current selection and state are conveyed by text, position, borders, icons, and labels as well as colour;
- responsive composition covers desktop, 980px, 680px, 420px, 390px, and 320px classes;
- reduced motion and visible focus remain first-class;
- the route-specific stylesheet owns no literal colour palette.

## Runtime boundary

This is a static Visual Language laboratory. No visible capture, approval, handoff, claim, project mutation, provider effect, publication, persistence, host operation, CLI command, or external read-back is connected. Consequential controls are disabled or explicitly labelled as specimens.
